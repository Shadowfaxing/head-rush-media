@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap');

:root {
    --primary-color: #FFD700;
    /* Vibrant Yellow */
    --bg-color: #0A0A0A;
    /* Deep Black */
    --bg-secondary: #000000;
    /* Ultra Black */
    --bg-tertiary: #111111;
    --text-color: #FFFFFF;
    --text-secondary: #AAAAAA;
    --accent-glow: rgba(255, 215, 0, 0.4);
    --transition-slow: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    --transition-fast: all 0.3s ease;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: 'Outfit', sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    line-height: 1.6;
    overflow-x: hidden;
    cursor: none;
    /* Hide default cursor */
}

/* Custom Scrollbar */
::-webkit-scrollbar {
    width: 8px;
}

::-webkit-scrollbar-track {
    background: var(--bg-color);
}

::-webkit-scrollbar-thumb {
    background: var(--bg-secondary);
    border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--primary-color);
}

/* Typography */
h1,
h2,
h3,
h4 {
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 1rem;
}

a {
    text-decoration: none;
    color: inherit;
    transition: var(--transition);
}

ul {
    list-style: none;
}

img,
video {
    max-width: 100%;
    height: auto;
    display: block;
}

/* Utilities */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
}

.text-yellow {
    color: var(--primary-color);
}

.bg-yellow {
    background-color: var(--primary-color);
}

.btn {
    display: inline-block;
    padding: 1rem 2rem;
    font-weight: 700;
    text-transform: uppercase;
    border-radius: 4px;
    cursor: pointer;
    transition: var(--transition);
    border: none;
}

.btn-primary {
    color: #090909;
    padding: 0.7em 1.7em;
    font-size: 18px;
    border-radius: 0.5em;
    background: #e8e8e8;
    cursor: pointer;
    border: 1px solid #e8e8e8;
    transition: all 0.3s;
    box-shadow: 6px 6px 12px #c5c5c5, -6px -6px 12px #ffffff;
    /* Ensure text is readable/standardized if needed, but keeping user snippet priority */
    font-weight: 700;
    /* Keeping bold from original theme for consistency */
}

.btn-primary:hover {
    border: 1px solid white;
    background: #e8e8e8;
    /* Maintain bg */
    transform: translateY(-2px);
    /* Keep slight lift if desired, or remove to match snippet strictly? Snippet has no transform. I will remove transform to match snippet strictly initially. */
    transform: none;
}

.btn-primary:active {
    box-shadow: 4px 4px 12px #c5c5c5, -4px -4px 12px #ffffff;
}

.btn-outline {
    background: transparent;
    border: 2px solid var(--primary-color);
    color: var(--primary-color);
}

.btn-outline:hover {
    background: var(--primary-color);
    color: #000;
}

/* Sections */
section {
    padding: 100px 0;
}

.section-header {
    text-align: center;
    margin-bottom: 4rem;
}

.section-header span {
    color: var(--primary-color);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.9rem;
    letter-spacing: 3px;
}

.section-header h2 {
    font-size: 2.5rem;
    margin-top: 0.5rem;
}

/* Navigation */
/* Ultimate Fusion Styles */
.shutter {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #000;
    z-index: 10000;
    pointer-events: none;
    transition: transform 1.2s cubic-bezier(0.85, 0, 0.15, 1);
}

.shutter::before,
.shutter::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 50%;
    background: #000;
    transition: transform 1.2s cubic-bezier(0.85, 0, 0.15, 1);
}

.shutter::before {
    top: 0;
}

.shutter::after {
    bottom: 0;
}

.shutter.open::before {
    transform: translateY(-100%);
}

.shutter.open::after {
    transform: translateY(100%);
}

.custom-cursor {
    position: fixed;
    top: 0;
    left: 0;
    width: 40px;
    height: 40px;
    background: var(--accent-glow);
    border: 1px solid var(--primary-color);
    border-radius: 50%;
    pointer-events: none;
    z-index: 100000;
    transform: translate(-50%, -50%);
    pointer-events: none;
    transition: width 0.3s, height 0.3s, background 0.3s;
    mix-blend-mode: difference;
}

.custom-cursor.hover {
    width: 100px;
    height: 100px;
    background: var(--primary-color);
}

.navbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    padding: 2.5rem 0;
    transition: var(--transition-slow);
}

.navbar.scrolled {
    background-color: rgba(10, 10, 10, 0.95);
    backdrop-filter: blur(10px);
    padding: 1rem 0;
    border-bottom: 1px solid rgba(255, 215, 0, 0.1);
}

.nav-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo img {
    height: 120px;
    /* Boosted logo size */
    width: auto;
    filter: drop-shadow(0 0 10px rgba(0, 0, 0, 0.5));
}

.nav-links {
    display: flex;
    align-items: center;
    gap: 2.5rem;
}

.nav-links a {
    font-weight: 600;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.nav-links a:hover {
    color: var(--primary-color);
}

.menu-toggle {
    display: none;
    cursor: pointer;
    color: var(--primary-color);
}

/* Hero Section - Flair Upgrade */
.hero {
    height: 95vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 0;
    margin: 10px;
    border-radius: 0 0 100px 100px;
    /* Rounded corners like inspiration */
    background: #000;
}

.hero-video-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
}

.unicorn-bg {
    width: 100% !important;
    height: 100% !important;
}

/* Hide Unicorn Studio Branding - Nuclear Option */
/* Parallax Hero Image */
.hero-image-parallax {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('assets/hero Photos/GT3 RS_Zwarkops-04112.jpg');
    background-size: cover;
    background-position: center;
    z-index: 0;
    transform: scale(1.1);
    /* Zoomed in to allow for movement edges */
    transition: transform 0.1s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    /* Smooth follow */
    will-change: transform;
}

#hero-bg-red {
    background-image: url('assets/hero Photos/GT3 RS_Zwarkops-04112.jpg');
    mix-blend-mode: screen;
    filter: opacity(0.5) saturate(200%) hue-rotate(-10deg) blur(2px);
    z-index: 2;
    /* Red/Cyan split simulation */
    pointer-events: none;
    -webkit-mask-image: radial-gradient(circle 0px at 0 0, black, transparent);
    mask-image: radial-gradient(circle 0px at 0 0, black, transparent);
}

#hero-bg-blue {
    background-image: url('assets/hero Photos/GT3 RS_Zwarkops-04112.jpg');
    mix-blend-mode: screen;
    filter: opacity(0.5) saturate(200%) hue-rotate(190deg) blur(2px);
    z-index: 3;
    /* Cyan/Red split simulation */
    pointer-events: none;
    -webkit-mask-image: radial-gradient(circle 0px at 0 0, black, transparent);
    mask-image: radial-gradient(circle 0px at 0 0, black, transparent);
}

/* Cleanup: hidden unicorn styles removed */

.hero-overlay.flair-gradient {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.95) 0%, rgba(139, 0, 0, 0.2) 50%, rgba(255, 140, 0, 0.4) 100%);
    pointer-events: none;
}

.hero-content-flair {
    z-index: 2;
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    align-items: center;
    gap: 4rem;
    width: 100%;
    text-align: left;
}

.hero-intro {
    display: block;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--primary-color);
    margin-bottom: 1rem;
    letter-spacing: 2px;
}

.hero-title {
    font-size: 6rem;
    line-height: 0.9;
    letter-spacing: -2px;
    margin: 0;
}

.hero-subtitle {
    font-size: 1.5rem;
    line-height: 1.2;
    color: var(--text-color);
    margin-bottom: 2.5rem;
    max-width: 400px;
}

.hero-btns-flair .btn {
    /* padding: 1.25rem 2.5rem; -- Let new style dictate padding */
    /* border-radius: 50px; -- Let new style dictate shape */
    display: flex;
    align-items: center;
    gap: 1rem;
    font-weight: 700;
    transition: var(--transition-slow);
}

.hero-btns-flair .btn:hover {
    transform: scale(1.05) translateX(10px);
}

/* Hero Footer Labels */
.hero-footer-labels {
    position: absolute;
    bottom: 60px;
    left: 10%;
    right: 10%;
    display: flex;
    justify-content: space-between;
    z-index: 2;
}

.footer-label {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.label-num {
    font-size: 0.8rem;
    font-weight: 800;
    color: var(--primary-color);
    letter-spacing: 1px;
}

.label-text {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-color);
    text-transform: capitalize;
}

/* Trusted By Bar */
.trusted-by {
    padding: 40px 0;
    background: #000;
}

.trusted-content {
    display: flex;
    align-items: center;
    gap: 3rem;
}

.trusted-text {
    font-size: 0.9rem;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 2px;
    max-width: 200px;
}

.brand-row {
    display: flex;
    align-items: center;
    gap: 4rem;
    flex-wrap: wrap;
}

.brand-logo {
    height: 40px;
    opacity: 0.5;
    transition: var(--transition-fast);
}

.brand-logo:hover {
    opacity: 1;
    filter: none;
}

.grayscale {
    filter: grayscale(1) invert(1);
}

/* Animations */
.fade-in {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 1s ease, transform 1s ease;
}

.fade-in.visible {
    opacity: 1;
    transform: translateY(0);
}

.delay-1 {
    transition-delay: 0.2s;
}

.delay-2 {
    transition-delay: 0.4s;
}

.scroll-indicator {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    opacity: 0.7;
}

.mouse {
    width: 25px;
    height: 40px;
    border: 2px solid white;
    border-radius: 20px;
    position: relative;
}

.mouse::before {
    content: '';
    width: 4px;
    height: 8px;
    background: var(--primary-color);
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    top: 8px;
    border-radius: 2px;
    animation: scroll 2s infinite;
}

@keyframes scroll {
    0% {
        opacity: 1;
        transform: translateX(-50%) translateY(0);
    }

    100% {
        opacity: 0;
        transform: translateX(-50%) translateY(15px);
    }
}

/* Services Grid */
.services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
}

.service-card {
    background: var(--bg-secondary);
    padding: 3rem 2rem;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: var(--transition);
    opacity: 0;
    transform: translateY(20px);
}

.service-card.visible {
    opacity: 1;
    transform: translateY(0);
}

.service-card:hover {
    border-color: var(--primary-color);
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
}

.service-card i {
    width: 50px;
    height: 50px;
    margin-bottom: 1.5rem;
}

.service-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
}

.service-card p {
    color: var(--text-secondary);
}

/* Portfolio */
.portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 1.5rem;
}

.portfolio-item {
    position: relative;
    border-radius: 8px;
    overflow: hidden;
    height: 450px;
    opacity: 0;
}

.portfolio-item.visible {
    opacity: 1;
}

.portfolio-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.8s ease;
}

.portfolio-info {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 2rem;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
    transform: translateY(20px);
    opacity: 0;
    transition: var(--transition);
}

.portfolio-item:hover img {
    transform: scale(1.1);
}

.portfolio-item:hover .portfolio-info {
    transform: translateY(0);
    opacity: 1;
}

.portfolio-cta {
    text-align: center;
    margin-top: 4rem;
}

/* Contact */
.contact-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    background: var(--bg-secondary);
    padding: 4rem;
    border-radius: 12px;
    opacity: 0;
}

.contact-wrapper.visible {
    opacity: 1;
}

.contact-info h2 {
    font-size: 3rem;
    margin-bottom: 1.5rem;
}

.contact-details {
    margin-top: 3rem;
}

.detail-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
}

.detail-item i {
    color: var(--primary-color);
}

.contact-form .form-group {
    margin-bottom: 1.5rem;
}

.contact-form input,
.contact-form select,
.contact-form textarea {
    width: 100%;
    padding: 1rem;
    background: #252525;
    border: 1px solid #333;
    color: white;
    border-radius: 4px;
    font-family: inherit;
    font-size: 1rem;
}

.contact-form input:focus,
.contact-form select:focus,
.contact-form textarea:focus {
    outline: none;
    border-color: var(--primary-color);
}

/* Footer */
footer {
    padding: 80px 0 30px;
    background: #050505;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.footer-content {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 4rem;
    margin-bottom: 4rem;
}

.footer-logo img {
    height: 40px;
    margin-bottom: 1.5rem;
}

.footer-logo p {
    color: var(--text-secondary);
    max-width: 300px;
}

.footer-links h4,
.footer-social h4 {
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    color: white;
}

.footer-links a {
    display: block;
    margin-bottom: 0.75rem;
    color: var(--text-secondary);
}

.social-icons {
    display: flex;
    gap: 1.5rem;
}

.social-icons i {
    transition: var(--transition);
}

.social-icons a:hover i {
    color: var(--primary-color);
    transform: translateY(-3px);
}

.footer-bottom {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    color: var(--text-secondary);
    font-size: 0.9rem;
}

/* Responsive Overrides */
@media (max-width: 992px) {
    .container {
        padding: 0 1.5rem;
    }

    .hero h1 {
        font-size: 3.5rem;
    }

    .contact-wrapper {
        grid-template-columns: 1fr;
        gap: 3rem;
        padding: 2rem;
    }

    .footer-content {
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
    }
}

@media (max-width: 768px) {
    .nav-links {
        position: fixed;
        top: 0;
        right: -100%;
        width: 100%;
        height: 100vh;
        background: var(--bg-color);
        flex-direction: column;
        justify-content: center;
        transition: 0.5s;
        z-index: 999;
    }

    .nav-links.active {
        right: 0;
    }

    .menu-toggle {
        display: block;
        position: relative;
        z-index: 1001;
    }

    .hero h1 {
        font-size: 2.8rem;
    }

    .hero-btns {
        flex-direction: column;
    }

    .footer-content {
        grid-template-columns: 1fr;
        text-align: center;
    }

    .footer-logo p {
        margin: 0 auto 1.5rem;
    }

    .social-icons {
        justify-content: center;
    }
}